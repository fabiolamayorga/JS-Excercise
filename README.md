## JS-Excercise - Gallery

Gallery Created with ES6

* To run this use `npm install`
* Then run `npm start`

Everything else is already in dist folder. In case that the dist folder is empty, you should run:
* `npm run webpack`
